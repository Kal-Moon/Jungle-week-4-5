3장

우리가 왜 기계어 코드를 배워야 하는 이유에 대해 설명해준다.
=> 어셈블리 코드는 컴퓨터 기계에 매우 의존적이다.
why? 특정 CPU의 스마트폰 또는 컴퓨터 속에 들어있는 실제 하드웨어 구조를 그대로 글자로 옮겨 적었기 때문

컴파일러(인간어를 기계어로 번역해주는 과정을 돕는 프로그램)로 어셈블리 코드를 만들어 내지만, 그 코드를 읽고 이해할 수 있는 것은 매우 중요하다.
컴파일러의 최적화 성능을 알 수 있으며, 코드에 내재된 비효율성을 분석할 수도 있다.
    Q. 왜 책에서는 최적화를 강조하는가. 그리고 처음부터 최적화된 하드웨어나 소프트웨어를 만들면 되지 않나?
        A. 최적화를 중요시 여기는 이유는 PC의 한정된 자원을 최대한 효율적으로 사용하기 위해서(흔히 뽕뽑는다라는 표현).
        그리고 처음부터 최적화된 프로그램을 만들 수 없는 이유는 사용자의 다양한 needs를 전부 맞출 수 없기 때문에(서울에서 부산을 간다 하더라도 어떻게 가는지는 전부 같지 않은 거 처럼)

3.1 역사적 관점
Intel CPU의 역사를 소개함. 8086 최초의 단일 칩 16비트 마이크로프로세서부터 Core i7까지의 역사를 설명함
-인스트럭션 집합에는 이러한 진화적인 유산 때문에 많은 이상한 잔재기능들이 존재한다는 것을 설명하기 위해

3.2 프로그램의 인코딩
파일이 어떻게 인코딩 되는지 알려주는 과정
linux> gcc -Og -o p p1.c p2.c
사용자가 저렇게 쉘(linux OS의 쉘)에서 명령어를 입력하게 되면 컴파일 시스템을 통해 인코딩이 된다.(실제 사용자가 입력하는 것은 gcc부터다)
gcc: 컴파일러  og:커맨드라인 옵션. 최적화 수준을 g로 하라는 것(g 대신 0, 1, 2, 3, s가 올 수 있음)   o p: 출력 파일명(출력 파일명을 p로)    p1.c p2.c: 컴파일할 소스 파일들
순서
    1. C 전처리기가 #include로 명시된 파일을 코드에 삽입해 주고, #define으로 선언된 매크로를 확장해 줌
    2. 컴파일러는 두 개의 소스 파일의 어셈블리 버전인 p1.s와 p2.s를 생성함
    3. 어셈블러는 어셈블리 코드를 바이너리 목적코드인 p1.o와 p2.o로 변환함
    4. 링커는 두 개의 목적코드 파일을 라이브러리 함수들을 구현한 코드와 함께 합쳐서 최종 실행파일인 p를 생성함

3.2.1 기계수준 코드
컴퓨터 시스템 추상화 모델의 중요성
    1. 기계수준 프로그램의 형식과 동작은 인스트럭션 집합구조(ISA)에 의해 정의됨
        ISA는 프로세서의 상태, 인스트럭션의 형식, 프로세서 상태에 대한 각 인스트럭션들의 영향들을 정의함
    2. 기계수준 프로그램이 사용하는 주소는 가상주소이며, 메모리가 매우 큰 바이트 배열인 것처럼 보이게 하는 메모리 모델을 제공함

C언어 코드와 기계수준 코드의 차이
    1. C가 다른 종류의 데이터 타입(int, float, str같은 타입)을 선언하고 메모리에 할당할 수 있는 모델을 제공하는 반면, 기계어 코드는 메모리를 단순힣 바이트 주소지정이 가능한 큰 배열로 봄
    2. 프로그램 메모리는 프로그램의 실행 기계어 코드, 운영체제를 위한 일부 정보, 프로시저 호출과 리턴을 관맇나느 런타임 스택, 사용자에 의해 할당된 메모리 블록들을 포함
    3. 하나의 기계어 인스트럭션은 매우 기초적인 동작만을 수행함

3.2.2 코드 예제
-S, -E, -c: gcc 실행 중 각 단계 사이에서 멈추고 그 중간 결과물만 보고 싶을 때 사용하는 옵션
-S: 컴파일 단계만 수행하고, 어셈블러는 호출하지 않음. .s파일을 보여줌
-E: 전처리까지 수행하고, .i파일을 보여줌
-c: 어셈블까지 수행하고, .o파일을 보여줌
    -d: 역어셈블러. 역어셈블하라는 것으로 기계어(바이너리)에서 사람이 읽을 수 있는 어셈블리어 형태로 가게 함(gcc 옵션이 아닌 objdump 옵션)
        Q. 역어셈블러는 왜 쓰는가?
            A. .o파일이나 최종 실행 파일은 순수 바이너리라서 사람이 봐도 알 수 없음. 안에 어떤 인스트럭션들이 들어있는지 들여다 보기 위해서
                Q. 그럼 왜 보나?
                    A. 1. 소스 코드가 없을 때. 남이 만든 라이브러리나 오래된 프로그램 또는 소스가 유실된 실행 파일만 가지고 있을 때, 프로그램이 무엇을 하는지 알아내기 위해(이걸 역엔지니어링이라 부름)
                   2. 링커가 실제로 무슨 짓을 했는지 확인하기 위해. 주소가 어떻게 채워졌는지, 코드가 어디로 재배치되었는지 확인 가능
                   3. 디버깅 할 때. 프로그램이 이상하게 동작하거나 크래시 났을 때, 소스 코드 수준의 디버거로 원인을 못 찾을 때 사용

컴퓨터에 의해 실제 실행된 프로그램은 단순히 일련의 인스트럭션을 인코딩한 일련의 바이트일 뿐이고, 컴퓨터는 그 인스트럭션이 어떤 소스 코드에서 나왔는지 알지 못함
    => 즉 소스 코드의 변수 이름, 타입 정보 같은 건 이 단계에서 이미 사라져있다는 걸 말함

기계어 코드의 특징
    1. 인스트럭션마다 길이가 다름
    2. 길이가 다르다 하더라도 어디서 끊어야하는지 헷갈리지 않음
    3. 역어셈블러는 기계어 코드 파일의 바이트 순서에만 전적으로 의존해서 어셈블리 코드를 결정
    4. 역어셈블러는 GCC가 생성한 어셈블리 코드와는 약간 다른 명명법을 인스트럭션에 사용
        => 기계어 길이는 다르지만 나름의 규칙이 있어 정확히 잘라 읽고, 규칙만 알면 바이트만 갖고 완전하게 복원 가능

링킹 전(.o)와 링킹 후(실행 파일)차이
    1. 주소가 바뀜. .o파일에서는 함수가 상대 위치였으나 링킹이 끝나면 실체 메모리 주소로 바뀜. 링커가 코드를 최종 프로그램의 특정 위치에 배치함
    2. callq 인스트럭션의 목적지가 채워짐. 실제 바이트 값까지 계산해서 검증해볼 수 있음 => 링커가 실제 거리를 계산해 그 값을 인스트럭션 바이트 안에 박아넣음
    3. 원래 없던 nop. 아무일도 안하는 명령어로 함수의 전체 코드 길이를 16바이트로 딱 맞추기 위해서
            why? 메모리 시스템(캐시 등)입장에서 다음 코드 블록이 특정 바이트 경계에 정렬되어 있으면 접근 성능이 더 좋아지기 때문에 링커/어셈블러가 일부러 의미없는 채움 바이트를 끼워넣음
    
        => 링커는 단순히 파일들을 이어붙이기만 하는 게 아니라 다음과 같은 역할을 하면서 능동적인 작업을 함

3.2.3 형식에 대한 설명
'.'으로 시작하는 몯느 라인은 어셈블러와 링커에 지시하기 위한 디렉티브(directive)들임
pushq %rbx: %rbx는 callee-saved 레지스터. 호출된 함수가 이 레지스터를 건드리려면 호출자가 원래 값을 그대로 돌려받는다는 걸 보장해줘야 함
movq %rdx, %rbx: 세번째 인자(dest)가 %rbx에 들어오는데 mult2를 호출하면 %rbx가 그 함수 내부에서 다른 값으로 덮어질 수 있어, 나중에 결과를 저장할 위치(dest)를 안전한 곳(%rbx)에 미리 옮겨두는 것
call mult2: 함수 호출이니 C코드의 mult2(x, y)호출에 대응
movq %rax, (%rbx): 함수 리턴값은 항상 %rax에 담기는 규약이고, (%rbx)는 %rax가 가리키는 메모리 주소에 값을 쓰는 것. 즉 *dest = ...에 대응

3.3 데이터의 형식
워드: 인텔 프로세서들이 16비트 데이터 타입을 말할 때 사용
더블워드: 워드에 기초해 32비트의 양을 부름
쿼드워드: 64비트의 양

C자료형의 길이
Char은 byte type으로 1바이트를 사용함(64비트건 32비트건 상관없이 1바이트 사용)
그 외 64비트 머신에서는 표처럼 길이를 갖고 32비트 머신에서는 long과 포인터는 1/2 size를 갖는다.
    Q. 포인터는 왜 8바이트의 길이를 갖나?
        A. 포인터에 들어있는 값은 결국 메모리 주소고, 그 주소가 표현할 수 있는 범위는 머신의 주소 공간 크기에 달려있기 때문

데이터 이동 인스트럭션 유형
    1. movb: 바이트 이동
    2. movw: 워드 이동
    3. movl: 더블워드 이동      - 집미어 l은 더블워드의 경우에 사용되는데, 32비트 양이 long word로 간주되기 때문
    4. movq: 쿼드워드 이동
    -- movabsq: 임의의 64비트 상수를 레지스터로 옮기는 인스트럭션. 목적지는 레지스터만 가능(메모리 불가). movq는 32비트 상수까지만 담을 수 있어 64비트 상수 전체가 필요할 때만 movabsq를 사용

3.4 정보 접근하기
정수 레지스터 표 간단설명
맨 위 숫자 63, 31, 15, 7 - 비트. 64, 32, 16, 8비트임.
    Q. 왜 64비트가 아니라 63으로 썼나?
        A. 비트 위치가 0번부터 세기 때문
%rax-%r15: 역사적 퇴적의 흔적임.
원래 8086시절부터 있던 8개(rax, rbx, rcx, rdx, rsi, rdi, rbp, rsp)는 각각 accumulator(누산기), base(기준), counter(카운터), data(데이터), source/destination index(원본/ 목적지 인덱스), base/stack pointer(기준/스택 포인터)처럼 8086때 부여받은 원래 용도를 반영한 이름
그게 32비트(eax...), 64비트(rax...)로 확장될 때마다 접두사만 e에서 r로 바뀌면서 이름의 뼈대는 계속 유지
%r8부터는 x86-64부터 만들어진 것으로 기존 8-32비트 머신은 사용 불가능

컴파일러들이 서로 약속한 관례
인자 전달용(1st-6th argument): 함수를 호출할 때 넘기는 인자들을 %rdi, %rsi, %rdx, %rcx, %r8, %r9 순서로 최대 6개까지 이 레지스터들에 담아서 넘긴다는 약속
Return value: 함수가 계산을 끝내고 결과를 돌려줄 때 항상 %rax에 담아서 돌려준다는 약속
Callee saved: %rbx, %rbp, %r12-%r15로 표시된 레지스터는 이걸 쓰려는 함수(callee. 호출당하는 쪽)가 책임지고 원래 값을 저장(push)했다 끝나기 전 복원(pop)해야함
Caller saved: %r10, %r11로 표시된 레지스터는 호출당하는 함수(callee)는 이 레지스터를 마음대로 덮어써도 되고 복원할 의무가 없음.

3.4.1 오퍼랜드 식별자(specifier)
오퍼랜드의 세 가지 타입
    1. immefiate: 상수값을 말함. ATT형식의 어셈블리 코드에서 상수는 $기호 다음에 C표준 서식을 사용하는 정수
    2. register: 레지스터의 내용. 각각 16개의 64비트, 32비트, 16비트, 8비트 레지스터들의 하위 일부분이 8, 4, 2, 1바이트 중 하나의 레지스터를 가리킴
    3. 메모리 참조: 유효주소(effective address)라고 부르는 계산된 주소에 의해 메모리 위치에 접근하게 됌

3.4.2 데이터 이동 인스트럭션
가장 많이 사용되는 인스트럭션은 데이터를 한 위치에서 다른 위치로 복사하는 명령
MOV클래스: 데이터 이동 인스트럭션으로 소스 위치에서 데이터를 목적지 위치로 어떤 변환도 하지 않고 복사함
          movb, movw, movl, movq 모두 유사한 효과를 가지고 있으며, 이들은 주로 이들이 서로 다른 크기의 데이터에 대해 계산한다는 점에서 다름. 1, 2, 4, 8바이트

메모리 위치에서 다른 위치로 어떤 값을 복사하기 위해서는 두 개의 인스트럭션이 필요
    1. 소스 값을 레지스터에 적재하는 인스트럭션
    2. 이 레지스터의 값을 목적지에 쓰기 위한 인스트럭션
오퍼랜드가 레지스터(%al, %eax, %rax같은 하위 이름)라면 그 레지스터의 크기와 접미사는 반드시 일치해야 함
대부분의 경우 MOV 인스트럭션들은 특정 레지스터 바이트들이나 대상 오퍼랜드에 의해 지정된 메모리 위치만 업데이트 하지만, movl은 예외적으로 레지트서를 목적지로 갖는 경우로 이 경우 레지스터의 상위 4바이트도 0으로 설정함
                                                                                                        why? 관습에 의해 생긴 것으로 x86-64에서 채택됨

MOVZ: 목적지의 남은 바이트들을 모두 0으로 채워줌
MOVS: 소스 오퍼랜드의 가장 중요한 비트를 반복해서 복사하는 부호 확장으로 채움

0으로 확장하는 데이터 이동 인스트럭션
1. MOVZ
2. movzbw: 목적지의 남은 바이트들을 0으로 채운 후 워드로 확장
3. movzbl: 목적지의 남은 바이트를 0으로 채운 후 더블워드로 확장
4. movzwl: 목적지의 남은 워드를 0으로 채운 후 더블워드로 확장
5. movzbq: 목적지의 남은 바이트를 0으로 채운 후 쿼드워드로 확장
6. movzwq: 목적지의 남은 워드를 0으로 채운 후 쿼드워드로 확장


--movslq - 모적지의 남은 더블워드를 부호비트로 채운 후 쿼드워드로 확장(MOVS(부호 확장)계열. MOVZ계열 아님)

    Q. 이걸 왜 하는 건가?
        A. 나머지 비트에 원래 있던 쓰레기값이 그래도 남아있으면 확장된 크기에서 완전히 달느 숫자로 읽혀 값이 훼손되기 때문에.
            즉, 데이터 값이 훼손되는 것을 방지하기 위해 0으로 밀어버린 후 그 자리에 원래 소스 값을 채워 넣는 방식

3.4.3 데이터 이동 예제
C언어에서 '포인터'라 부르는 것이 어셈블리어에서는 단순히 주소임
포인터를 역참조하는 것은 포인터를 레지스터에 복사하고, 이 레지스터를 메모리 참조에 사용하는 과정으로 이루어짐
x같은 지역변수들은 메모리에 저장되기보다는 종종 레지스터에 저장됨
레지스터의 접근은 메모리보다 속도가 훨씬 더 빠름 => 컴파일러가 가능하면 변수를 레지스터에 두려고 함

pushq: 쿼드워드(8바이트)만큼 줄인다음 그 새 위치에 값을 저장하는 인스트럭션
popq: 가리키는 위치에서 값을 읽은 다음 쿼드워드만큼 늘리는 인스트럭션

3.4.4 스택 데이터의 저장과 추출 push, pop
스택은 프로시저 호출을 처리하는데 중요한 역할을 함
스택은 값들이 후입선출(LiFO)형태로만 추가되거나 제거되는 자료구조
push 연산으로 스택에 데이터를 추가, pop 연산으로 제거하되, 제거하는 값이 가장 최근에 추가된 값이어야 하며, 그 값은 스택에 여전히 남아 있음
스택은 배열로 구현될 수 있고, 배열에서 원소들의 배열 한쪽 끝에서만 추가하거나 제거되는데, 이 끝 부분을 스택의 top이라 부름
popq 인스트럭션은 데이터를 추출하지만 pushq 인스트럭션은 데이터를 스택에 추가하는 기능을 제공함 => 두 인스트럭션은 각각 두 개의 동작을 하나로 압축한 인스트럭션
이들은 한 개의 오퍼랜드를 사용함(추가할 소스 데이터와 추출을 위한 데이터 목적지)

3.5 산술연산과 논리연산
오퍼랜드의 길이에 따른 다양한 변형이 가능하기에 대부분의 연산을 인스트럭션 클래스에 따라 나열함(leaq만은 길이에 따른 변형이 없음)
    Q. 왜 leaq만 변형이 없나?
        A.leaq만 만들어낸 결과물은 데이터 값이 아닌 항상 메모리 주소 즉 포인터임. 아키텍처에서 주소라는 것은 애초에 크기가 64비트로 고정되어있어 그릇의 크기는 항상 8바이트로 동일함
각 인스트럭션 클래스는 이들 네 개의 서로 다른 크기의 데이터 연산을 갖음
-유효주소 적재, 단항(unary), 이항(binary), 쉬프트
이항 연산은 두 개의 오퍼랜드를 가지는 반면, 단항 연산은 한 개의 오퍼랜드를 갖음

3.5.1 유효주소 적재 Load Effecitve Address
leaq: 유효주소 적재 인스트럭션. 실제로는 movq 인스트럭션의 변형임
메모리 참조처럼 생긴 주소 계산식을 실제로는 메모리에 접근하지 않고, 그 계산된 주소값 자체를 레지스터에 담아주는 인스트럭션
오퍼랜드는 일종의 메모리 참조처럼 보이지만, 가리키는 위치에서 읽기를 수행하는 대신에 유효주소를 목적지에 복사함
일반적인 산술연산을 간결하게 설명하는 역할을 함
목적 오퍼랜드는 반드시 레지스터만 가져올 수 있음

    Q. leaq의 기능은 뭔가?
        A. 1. 원래 목적인 포인터 생성
           2. leaq의 주소 계산 능력 자체를 일반 산술 계산기로 빌려 쓰는 것 > 이렇게 하면 곱센 인스트럭션보다 빠르고, 곱셈+덧셈을 한 번에 처리할 수 있어 컴파일러가 선호함
            => 메모리 주소 문법으로 적힌 수식을 계산기처럼 계산해서, 그 결과 숫자(주소 또는 산술값)를 레지스터에 저장하는 인스트럭션

3.5.2 단항 및 이항 연산
단항 연산: 하나의 오퍼랜드가 소스와 목적지로 동시에 사용됨(INC, DEC, NEG, NOT)
incq(%rsp)는 메모리 위치의 값을 읽어서 1을 더하고, 다시 그 자리에서 써넣음
C언어에서 ++나 --를 어셈블리로 구현할 때 이렇게 씀

이항 연산: 두 번째 오퍼랜드는 소스이면서 목적지로 사용됨(ADD, SUB, IMUL, XOR, OR, AND)
첫 번째 오퍼랜드는 상수나 레지스터, 메모리 위치가 올 수 있음. 두 번쨰는 레지스터나 메모리가 올 수 있음
C언에에서 할당 연산자인 x -= y와 같은 문장과 유사

MOV 인스트럭션에서처럼 두 개의 오퍼랜드가 모두(동시에) 메모리 위치가 될 수 없음.
두 번째 오퍼랜드가 메모리 위치일 때 프로세서가 메모리에서 값을 읽고, 연산을 하고, 그 결과를 다시 메모리에 써야함

3.5.3 쉬프트 연산
쉬프트하는 크기를 먼저 주고, 쉬프트할 값을 두 번째로 줌
산술과 논리형 우측 쉬프트가 모두 가능함
쉬프트할 양을 레지스터로 주고 싶을 때 오직 %cl(단일 바이트 레지스터)만 쓸 수 있음. 대부분의 레지스터는 자유롭게 쓰이나 쉬프트 양을 레지스터로 지정할 때만큼은 %cl만 쓸 수 있음
쉬프트 양은 무제한으로 적용되지 않음. 데이터 크기에 따라 잘려서(모듈로)적용됨   ex. w비트 데이터라면 %cl의 하위 m 비트만 쓰고(2^m=w), 나머지 상위 비트는 무시됨
좌측 쉬프트와 우측 쉬프트의 비대칭
돠측 쉬프트는 SAL과 SHL 두 이름이 있지만 사실 완전히 동일한 인스트럭션이고, 우측 쉬프트는 진짜 다른 두 가지가 있음
부호 없는/있는 데이터를 다루는 방식이 다르다는 큰 흐름(MOVZ vs MOVS와 완전히 같은 논리)이 쉬프트 연산에도 그대로 적용

3.5.4 토의
다뤘던 산술연산을 종합함.
대부분의 인스트럭션은 부호형이든 비부호형이든 똑같이 동작하고, 오직 우측 쉬프트만 SAR/SHR로 나뉘어서 부호를 구분함
    => 2의 보수 표현 방식 자체가 비트 패턴에 덧셈, 뺄셈, 곱셈 등을 그대로 적용하면, 그 결과를 부호형으로 읽든 비부호형으로 읽든 알아서 맞는 답이 나오도록 설계되어 있기 때문

3.5.5 특수 산술연산
두 개의 64비트 부호형 또는 비부호형 정수들 간의 곱셈은 결과 값을 표시하기 위해 128비트를 필요로 함 => x86-64는 이런 특수한 경우를 위해 제한적으로 128비트 연산을 지원
옥트워드(oct word):인텔이 16바이트 워드를 명명한 것
x86-64는 두 개의 다른 단일 오퍼랜드 곱셈 인스트럭션을 제공하며, 두 64비트 값의 완전한 128비트 곱을 계산함
하나는 비부호형(mulq), 다른 하나는 2의 보수(imulq)곱셈 => 이들 모두 한 개의 인자는 레지스터 %rax에 보관해야 하고, 다른 하나는 인스트럭션 소스 오퍼랜드로 주어짐
곱은 레지스터 %rdx(상위 64비트)와 %rax(하위 64비트)에 저장됨
imulq라는 이름은 두 종류의 다른 곱셈 연산에 사용되나 어셈블러는 오퍼랜드의 수에 따라 어느 경우에 해당되는지 알 수 있음